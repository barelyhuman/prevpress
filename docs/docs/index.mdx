## Introduction

`prevpress` is a tiny engine with utils to write content first websites with preact.

It's built to be able to convert all `.md` and `.mdx` files to their respective html files. 

### Quickstart

#### Installation

```sh
npm i prevpress
```

```sh
npx pre
```


